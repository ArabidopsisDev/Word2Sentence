<Window x:Class="Word2Sentence.OpenRouterSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:Word2Sentence.Localization"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="{loc:Loc SetupWindowTitle}" Width="780" Height="650"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="NoResize"
        Background="{StaticResource CanvasBrush}" ShowInTaskbar="False">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="0" GlassFrameThickness="0" CornerRadius="10" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="SetupChoiceButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ChoiceBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ChoiceBorder" Property="Background" Value="{StaticResource AccentSoftBrush}" />
                                <Setter TargetName="ChoiceBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </Window.Resources>

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" Background="{StaticResource CanvasBrush}">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="42" /><RowDefinition Height="*" /></Grid.RowDefinitions>

            <Border Background="#ECEAE4" CornerRadius="10,10,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Margin="16,0" Text="Word2Sentence · OpenRouter" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <Button HorizontalAlignment="Right" Style="{StaticResource CloseTitleBarButton}" Content="&#xE8BB;" Click="Close_Click" />
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="38,28,38,26">
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="58" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                    <ContentControl Style="{StaticResource LogoMark}" Width="48" Height="48" HorizontalAlignment="Left" VerticalAlignment="Top" />
                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                        <TextBlock Style="{StaticResource Eyebrow}" Text="{loc:Loc SetupEyebrow}" />
                        <TextBlock Margin="0,4,0,0" Text="{loc:Loc SetupTitle}" FontSize="25" FontWeight="SemiBold" />
                        <TextBlock Margin="0,6,0,0" Text="{loc:Loc SetupSubtitle}" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" LineHeight="20" />
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" Margin="0,26,0,18">
                    <StackPanel x:Name="ModeChoicePanel">
                        <TextBlock Text="{loc:Loc SetupChoosePath}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                        <Button Style="{StaticResource SetupChoiceButton}" Click="BeginnerMode_Click">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="46" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <Border Width="34" Height="34" CornerRadius="17" Background="{StaticResource AccentBrush}">
                                    <TextBlock Text="1" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{loc:Loc SetupBeginner}" FontSize="16" FontWeight="SemiBold" />
                                    <TextBlock Margin="0,5,0,0" Text="{loc:Loc SetupBeginnerDesc}" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" />
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="›" FontSize="26" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" />
                            </Grid>
                        </Button>
                        <Button Margin="0,12,0,0" Style="{StaticResource SetupChoiceButton}" Click="TechnicalMode_Click">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="46" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <Border Width="34" Height="34" CornerRadius="17" Background="{StaticResource SurfaceMutedBrush}">
                                    <TextBlock Text="&lt;/&gt;" FontFamily="Cascadia Mono, Consolas" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{loc:Loc SetupTechnical}" FontSize="16" FontWeight="SemiBold" />
                                    <TextBlock Margin="0,5,0,0" Text="{loc:Loc SetupTechnicalDesc}" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" />
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="›" FontSize="26" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" />
                            </Grid>
                        </Button>
                    </StackPanel>

                    <Grid x:Name="BeginnerPanel" Visibility="Collapsed">
                        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                        <TextBlock x:Name="BeginnerStepLabel" Style="{StaticResource Eyebrow}" />

                        <Grid Grid.Row="1" Margin="0,12,0,12">
                            <StackPanel x:Name="BeginnerStepOne">
                                <TextBlock Text="{loc:Loc SetupAccountTitle}" FontSize="20" FontWeight="SemiBold" />
                                <TextBlock Margin="0,9,0,0" Text="{loc:Loc SetupAccountDesc}" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" LineHeight="21" />
                                <Border Margin="0,18,0,0" Padding="16" CornerRadius="7" Background="{StaticResource AccentSoftBrush}">
                                    <TextBlock Text="{loc:Loc SetupAccountInstruction}" TextWrapping="Wrap" LineHeight="21" />
                                </Border>
                                <Button Margin="0,18,0,0" HorizontalAlignment="Left" Style="{StaticResource PrimaryButton}" Content="{loc:Loc OpenSignIn}" Click="OpenSignIn_Click" />
                            </StackPanel>
                            <StackPanel x:Name="BeginnerStepTwo" Visibility="Collapsed">
                                <TextBlock Text="{loc:Loc SetupCreditsTitle}" FontSize="20" FontWeight="SemiBold" />
                                <TextBlock Margin="0,6,0,0" Text="{loc:Loc SetupCreditsDesc}" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" LineHeight="19" />
                                <Border Margin="0,12,0,0" Padding="14" CornerRadius="7" Background="{StaticResource AccentSoftBrush}">
                                    <TextBlock Text="{loc:Loc SetupCreditsInstruction}" TextWrapping="Wrap" LineHeight="19" />
                                </Border>
                                <Button Margin="0,12,0,0" HorizontalAlignment="Left" Style="{StaticResource PrimaryButton}" Content="{loc:Loc OpenCredits}" Click="OpenCredits_Click" />
                            </StackPanel>
                            <StackPanel x:Name="BeginnerStepThree" Visibility="Collapsed">
                                <TextBlock Text="{loc:Loc SetupCreateKeyTitle}" FontSize="20" FontWeight="SemiBold" />
                                <TextBlock Margin="0,9,0,0" Text="{loc:Loc SetupCreateKeyDesc}" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" LineHeight="21" />
                                <Border Margin="0,18,0,0" Padding="16" CornerRadius="7" Background="{StaticResource AccentSoftBrush}">
                                    <TextBlock Text="{loc:Loc SetupCreateKeyInstruction}" TextWrapping="Wrap" LineHeight="21" />
                                </Border>
                                <Button Margin="0,18,0,0" HorizontalAlignment="Left" Style="{StaticResource PrimaryButton}" Content="{loc:Loc OpenKeys}" Click="OpenKeys_Click" />
                            </StackPanel>
                            <StackPanel x:Name="BeginnerStepFour" Visibility="Collapsed">
                                <TextBlock Text="{loc:Loc SetupConnectTitle}" FontSize="20" FontWeight="SemiBold" />
                                <TextBlock Margin="0,9,0,0" Text="{loc:Loc SetupConnectDesc}" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" LineHeight="21" />
                                <TextBlock Margin="0,18,0,6" Style="{StaticResource Eyebrow}" Text="{loc:Loc OpenRouterApiKey}" />
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <PasswordBox x:Name="BeginnerKeyBox" />
                                    <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Content="{loc:Loc PasteKey}" Click="PasteBeginnerKey_Click" />
                                </Grid>
                                <TextBlock Margin="0,8,0,0" Text="{loc:Loc KeyPrivacy}" Foreground="{StaticResource TextMutedBrush}" FontSize="12" TextWrapping="Wrap" />
                                <TextBlock x:Name="BeginnerStatusText" Margin="0,11,0,0" FontSize="12" TextWrapping="Wrap" />
                            </StackPanel>
                        </Grid>

                        <Grid Grid.Row="2">
                            <Button x:Name="BeginnerBackButton" HorizontalAlignment="Left" Style="{StaticResource SecondaryButton}" Content="{loc:Loc SetupBack}" Click="BeginnerBack_Click" />
                            <Button x:Name="BeginnerNextButton" HorizontalAlignment="Right" Style="{StaticResource PrimaryButton}" Content="{loc:Loc SetupNext}" Click="BeginnerNext_Click" />
                        </Grid>
                    </Grid>

                    <StackPanel x:Name="TechnicalPanel" Visibility="Collapsed">
                        <TextBlock Style="{StaticResource Eyebrow}" Text="{loc:Loc SetupTechnicalEyebrow}" />
                        <TextBlock Margin="0,7,0,0" Text="{loc:Loc SetupTechnicalTitle}" FontSize="20" FontWeight="SemiBold" />
                        <TextBlock Margin="0,9,0,0" Text="{loc:Loc SetupTechnicalInstruction}" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" LineHeight="21" />
                        <TextBlock Margin="0,20,0,6" Style="{StaticResource Eyebrow}" Text="{loc:Loc OpenRouterApiKey}" />
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <PasswordBox x:Name="TechnicalKeyBox" />
                            <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Content="{loc:Loc PasteKey}" Click="PasteTechnicalKey_Click" />
                        </Grid>
                        <TextBlock Margin="0,8,0,0" Text="{loc:Loc KeyPrivacy}" Foreground="{StaticResource TextMutedBrush}" FontSize="12" TextWrapping="Wrap" />
                        <TextBlock x:Name="TechnicalStatusText" Margin="0,11,0,0" FontSize="12" TextWrapping="Wrap" />
                        <Grid Margin="0,24,0,0">
                            <Button HorizontalAlignment="Left" Style="{StaticResource SecondaryButton}" Content="{loc:Loc SetupBack}" Click="BackToMode_Click" />
                            <Button x:Name="TechnicalValidateButton" HorizontalAlignment="Right" Style="{StaticResource PrimaryButton}" Content="{loc:Loc ValidateAndSave}" Click="ValidateTechnical_Click" />
                        </Grid>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="2">
                    <Button HorizontalAlignment="Left" Style="{StaticResource SecondaryButton}" Content="{loc:Loc SetupOffline}" Click="Offline_Click" />
                    <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" Text="{loc:Loc SetupOfficialOnly}" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
